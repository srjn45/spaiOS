# Task: Conversation Memory + Multi-Turn Context

**Date:** 2026-04-27
**Phase:** Phase 1 — Milestone 6
**Session goal:** Verify multi-turn history is wired correctly and add `/clear` command to reset context without hiding the overlay.
**Notion task ID:** phase1_m6_memory

## What We're Building

The orchestrator already maintains `_history` across turns. This session confirms that
contextual references (e.g. "move the .py file", "delete it") resolve correctly across
turns, and adds a `/clear` command so the user can reset context mid-session.

## Approach

1. Review `_history` handling in `orchestrator.py` — confirmed correct, user/assistant
   pairs are appended after each `ask()` and capped at `_MAX_HISTORY_PAIRS * 2` messages.
2. Intercept `/clear` in `_on_prompt_submitted` before spinning up an AskThread.
3. Call `orchestrator.clear_history()`, show "Context cleared" feedback, restore input.
4. Verify delete confirmation still works when path came from a prior turn (it does —
   `_pending_delete` is stored on the orchestrator, not derived from history).
5. Commit.

## Files Touched

- `src/spaiOS/ui/overlay.py` — add `/clear` interception in `_on_prompt_submitted`

## Done When

- Typing `/clear` in the input shows "Context cleared — starting fresh." and resets history
- Overlay stays visible after `/clear`
- Multi-turn flow works: list files → "move the .py file to scripts/" → success
- Delete confirmation works when the file was named in a prior turn
