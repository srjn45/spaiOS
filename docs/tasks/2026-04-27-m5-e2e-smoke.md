# Task: End-to-End Smoke Test & Polish

**Date:** 2026-04-27
**Phase:** Phase 1 — Milestone 5, Session 14
**Session goal:** Run the overlay live, exercise the full file agent pipeline, and fix any rough edges found during testing.
**Notion task ID:** phase1_m5_file_agent

## What We're Building

No new features — this session validates that the Orchestrator → File Agent → Qt UI pipeline works correctly end-to-end with real Ollama inference. We fix anything that breaks.

## Approach

1. Confirm sandbox files exist (done — rich set already present)
2. Launch overlay: `spaiOS`
3. Test "list my sandbox files" → AI calls list_directory, shows listing in overlay
4. Test "organize my sandbox" → AI creates folders, moves files
5. Test delete flow: ask AI to delete a file → amber sphere + yes/no → confirm both paths
6. Test "read summary of <file>" → AI shows preview
7. Tune system prompt if tool use is unreliable
8. Commit any fixes

## Files Touched

- `src/spaiOS/core/orchestrator.py` — system prompt tuning if needed
- `src/spaiOS/core/file_agent.py` — bug fixes if found
- `src/spaiOS/ui/overlay.py` — UI polish if needed

## Done When

- [x] "list my sandbox files" returns a visible listing
- [x] "organize my sandbox" creates at least one folder and moves files
- [x] Delete confirmation flow works: yes deletes, no cancels
- [x] "read summary of <file>" shows file content preview
- [x] No unhandled exceptions in the terminal during a normal session

## Fixes Applied

- Added `_BASE_SYSTEM_PROMPT` to `orchestrator.py` — model had zero guidance about tools when no screen context was captured
- Dropped `visible_text` from screen context injection — AT-SPI was picking up Chrome Remote Desktop session picker text, which overwhelmed `llama3.2:3b` and caused it to ignore tools entirely
- App name + window title are still injected as lightweight screen context
